set httpd port 2812 and
    use address 0.0.0.0
    allow admin:monit readonly
    
set mailserver localhost    
set alert alert@example.com but not on { instance }
set alert alert2@example.com but not on { instance }
